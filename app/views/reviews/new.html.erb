

<div class="container w-50 shadow py-4 rounded-lg">
  <h2>New Review</h2>
  <%= simple_form_for [@restaurant, @review] do |f| %>
    <%= f.input :content %>
    <%= f.input :rating %>
    <%= f.submit class: 'btn btn-primary' %>
  <% end %>

  <%= link_to "Back", restaurants_path %>
</div>
