
  <div class="container">
    <div class="banner">
      <div class="banner-info d-flex align-items-end">
        <h1 id="banner-title"><%= @restaurant.name %></h1>
        <p id="banner-btn"><%= link_to "Add a Review", new_restaurant_review_path(@restaurant.id), class: "btn btn-primary" %></p>
      </div>
    </div>
  </div>

<div class="container mb-5">
  <p><strong>Address:</strong> <%= @restaurant.address %></p>
  <p><strong>Phone number:</strong> <%= @restaurant.phone_number %></p>

  <%= link_to "Back", restaurants_path, class: "btn btn-primary" %>

  <h2 class="mt-4">Leve a review 🤩</h2>
  <%= simple_form_for [@restaurant, @review] do |f| %>
    <%= f.input :content %>
    <%= f.input :rating %>
    <%= f.submit class: 'btn btn-primary' %>
  <% end %>

  <h2 class="mt-4">Reviews 🤓</h2>
  <% @restaurant.reviews.each do |review| %>
    <li class="list-group-item">
    <%= review.content %> <%= review.rating %>
    </li>
  <% end %>
</div>
